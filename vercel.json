{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd web && npm install && npm run build",
  "installCommand": "echo \"defer to per-project install\"",
  "outputDirectory": "web/.next",
  "devCommand": "cd web && npm run dev"
}
